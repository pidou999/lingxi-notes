{
  "name": "ai-notes",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter @ai-notes/web dev",
    "build": "pnpm --filter @ai-notes/web build",
    "server": "cd apps/server && go run .",
    "server:build": "cd apps/server && go build -ldflags=\"-s -w\" -o lingxi-server.exe ."
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
