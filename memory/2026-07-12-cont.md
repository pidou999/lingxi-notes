# 2026-07-12 会话记忆（续）

## Cookie 验证结果

### 测试结论
- **Playwright + Cookie 方案概念上完全可行**（绕过 Akamai Bot Manager + zse_ck 挑战）
- 测试使用的 Cookie（来自 `web-scraper-notes/data/cookies.json`）已过期
- `z_c0` 的 timestamp 为 2026-07-07，当前 2026-07-12，需用户重新复制新鲜 Cookie
- 验证了：无需 Cookie → 返回 `zhihuBlocked` → 引导用户配置

### 导航策略改进
- 从 `waitUntil: 'commit'` 改为 `waitUntil: 'domcontentloaded'` — 更可靠
- 增加了页面对话性检测（`page.evaluate` 检查）— 防止页面关闭导致的崩溃
- 改进了 `waitForRealContent` 的错误处理
- 增加了"荒原页"检测（`title.includes('荒原') || title.includes('没有知识')`）— 及时提示 Cookie 过期

### 前端整合完成
- `ResultModal.tsx`：自动获取 `getCookieForUrl(url)` 传入 fetch-page API
- 知乎反爬提示更新为带链接的「配置知乎 Cookie」（点击跳转 `/settings`）
- 设置页已包含完整的 Cookie 管理 UI（之前已完成）
- 无 Cookie → 返回错误 + 提示 → 引导到设置页

### 待确认
- ☐ 用户需提供新鲜知乎 Cookie（含有效 `z_c0`）测试抓取效果
- ☐ 验证图片下载功能
- ☐ 长时间无活动后自动释放 browser 资源

## 清理
- 删除 `test-scraper-api.mjs`, `test-playwright.mjs`, `test-playwright2.mjs`
- 新增 `src/lib/scraper.ts` 的荒原页检测

## 关键决定
- `cookies.ts` + 设置页 UI 先前已完成，无需额外开发
- 图片下载逻辑保持现有 `fetch-utils.ts` 的 `downloadAllImages`
